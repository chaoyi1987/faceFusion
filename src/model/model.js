function model(){
  $(".model1").scc=({ "margin-top":"0.1rem",
  "border": "0.2rem solid #ffffff"})
  var sex = 1;
  console.log($(this.store.getCookie('code')));
  console.log();

  if($(this.store.getCookie('code'))==0){
    if(sex == 1){
    }
  }
}
